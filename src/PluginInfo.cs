namespace SeapowerMultiplayer
{
    internal static class PluginInfo
    {
        public const string PLUGIN_GUID    = "com.seapower.fourplayer";
        public const string PLUGIN_NAME    = "Sea Power Four Player";
        // BepInEx 5 requires a numeric System.Version-compatible value here.
        // Keep the alpha channel label in package/docs, not in BepInPlugin metadata.
        public const string PLUGIN_VERSION = "0.1.4";
    }
}
